/*
require(['core/first','jquery'], function($) {
    window.$ =$;
   require(['theme_ufsm2/bootstrap', 'core/log'], function(b, log) {
      //  log.debug('Bootstrap JavaScript initialised');

    });
});
*/